/** Shared between the Node session module and the Edge middleware. No runtime-specific imports. */
export const SESSION_COOKIE = "harbor_beta";
export const SESSION_TTL_MS = 30 * 24 * 60 * 60 * 1000; // 30 days
